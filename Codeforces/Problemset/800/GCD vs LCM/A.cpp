#include<iostream>
using namespace std;

int main(){
	cin.tie(0)->sync_with_stdio(0);
	int inputs, number;
	cin>>inputs;

	while(inputs--){
		cin>>number;
		cout<<(number-3)<<" "<<1<<" "<<1<<" "<<1<<endl;
	}
}