#include<bits/stdc++.h>
using namespace std;

int main(){
    cin.tie(0)->sync_with_stdio(0);

    int n;cin>>n;
    vector<int> nums(n,0);
    for(int i=0;i<n;i++) std::cin>>nums[i];

    vector<int> res(3,0);
    res[0]=0, res[1]=1, res[2]=2;
    for(int i=0; i<)

    return 0;
}